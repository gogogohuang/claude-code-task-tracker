# v0.20 Split (M4) Implementation Plan

> **For agentic workers:** TDD task-by-task. Checkboxes track progress.

**Goal:** `watch` 雙欄監看兩 session：活動句、血條、最多 5 行 task；`v` 進出、`[` `]` 焦點。

**Spec:** `docs/superpowers/specs/2026-09-17-v0.20-split-design.md`

## Global Constraints

- `MIN_SPLIT_COLUMNS = 120`、`SPLIT_TASK_ROWS = 5`
- 不 bump version；不改 hook
- commit 僅使用者要求時

---

### Task 1: `split-layout` 純函式

**Files:** `src/split-layout.ts`, `src/split-layout.test.ts`

- `MIN_SPLIT_COLUMNS`, `SPLIT_TASK_ROWS`
- `canEnterSplit(columns: number): boolean`
- `otherFocus(focus: "left" | "right"): "left" | "right"`
- `focusedSessionId(left, right, focus): string`
- `canPickSplitPartner(candidateId, leftId): boolean`（不可同 id）
- `SPLIT_TOO_NARROW_NOTICE` 常數文案

### Task 2: `visibleSplitTaskRows` + Split UI

**Files:** `src/split-tasks.ts` (+ test), `src/ui/SplitView.tsx`

- `visibleSplitTaskRows(rows, offset, limit=5)`
- `SplitView`：左右 `SplitPane`（標題▸、活動、血條、task 列）

### Task 3: App 接線

**Files:** `App.tsx`, `delete-session.ts` (+ test)

- `WatchView` 加 `"split"`；`shouldHandleDeleteKey` 在 split+有焦點 session 為 true
- state：`splitLeftId`/`splitRightId`/`splitFocus`/`splitReturnSessionId`/`pickingSplitPartner`
- `v`：main+已選 → 寬度檢查 → `pickingSplitPartner`；split 內 → 退出
- 選 B：進 split；同 id 拒絕
- `[` `]` 切焦點；`a/s/h/p/d` 打 `focusedSessionId`
- split 時讀／顯示左右 state（STATE_DIR change 已 bump revision 即可重讀）

### Task 4: README + roadmap

---

## Completion Criteria

- [x] 驗收項符合 design
- [x] `pnpm test` + `pnpm typecheck`
- [x] 不 bump version

### 完成紀錄

- `split-layout` / `split-tasks` + 測試
- `SplitView` 雙欄 UI
- `App`：`v` 選夥伴／進出、`[` `]`、焦點快捷鍵；`delete-session` 支援 split
- README + roadmap
