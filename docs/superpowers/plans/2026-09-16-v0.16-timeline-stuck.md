# v0.16 Timeline + Stuck Implementation Plan

> **For agentic workers:** Implement task-by-task with TDD. Steps use checkbox tracking.

**Goal:** In-memory activity timeline (`h`) + stuck label for long-running non-waiting tools.

**Status:** ✅ 完成（2026-09-16）  
**Spec:** `docs/superpowers/specs/2026-09-16-v0.16-timeline-stuck-design.md`

## Global Constraints

- TIMELINE_MAX=40；STUCK_MS=120_000；無落盤；不改 hook
- 快捷鍵 `h`；WatchView 加 `history`
- 不 bump version；commit 僅在使用者要求時

---

### Task 1: activity-timeline — ✅

- Create: `src/activity-timeline.ts`, `src/activity-timeline.test.ts`

### Task 2: activity-stuck — ✅

- Create: `src/activity-stuck.ts`, `src/activity-stuck.test.ts`

### Task 3: WatchView + HistoryPanel — ✅

- Modify: `src/delete-session.ts` (`"history"`)
- Create: `src/ui/HistoryPanel.tsx`

### Task 4: App / TaskList / README — ✅

- Modify: App, TaskList, README
- `pnpm test` + `pnpm typecheck` pass

## Completion Criteria

- [x] P4／P5 依 design
- [x] tests + typecheck
- [x] README 含 `h`、卡住、記憶體說明
