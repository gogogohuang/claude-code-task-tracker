# v0.21 Polish Implementation Plan

> TDD. Spec: `docs/superpowers/specs/2026-09-17-v0.21-polish-design.md`

**Goal:** E3 locale for activity sentences + E4 `c`/`C` clipboard. No T3.

## Tasks

1. `src/locale.ts` + test — `resolveLocale`
2. `describe-activity` — `locale?: "zh"|"en"`；既有測顯式 `zh`；加 en 抽樣測
3. `apply-event` — `describeActivity({ ..., locale: resolveLocale(process.env) })`
4. `clipboard.ts` + test；`App` `c`/`C`
5. README + roadmap

## Completion

- [x] `pnpm test` + `typecheck`
- [x] 不 bump version

### 完成紀錄

- `locale` / `clipboard` + 測試
- `describeActivity` 雙語；hook 接 `resolveLocale`
- App `c`／`C`；README／roadmap
