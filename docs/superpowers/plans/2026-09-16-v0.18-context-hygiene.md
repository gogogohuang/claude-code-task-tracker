# v0.18 Context Hygiene Implementation Plan

> **For agentic workers:** Implement task-by-task with TDD. Steps use checkbox tracking.

**Goal:** C2 inspect byte heat + C3 diet list (fat + repeated Read) + C4 heavy-baseline heat summary in Advice.

**Spec:** `docs/superpowers/specs/2026-09-16-v0.18-context-hygiene-design.md`

## Global Constraints

- Top-5；repeated-read ≥3；byte only；不 bump version；不改 hook
- commit 僅在使用者要求時

---

### Task 1: inspect heat helpers — 

- Create: `src/inspect/heat.ts`, `src/inspect/heat.test.ts`
- Modify: `types.ts` (`byteSize?`), `discover.ts` (enrich + sort), `InspectView.tsx`

### Task 2: transcript path + fat message —

- Modify: `tail-transcript.ts`, `types.ts` (`path?`), `detect.ts` fat message

### Task 3: repeated-read —

- Modify: `accumulate.ts` (`readPathCounts`), `detect.ts`, `types.ts` (`AdviceKind`)

### Task 4: C4 wire Advice —

- Modify: `Advice` detailLines；`AdvicePanel`；`App.tsx` cache discover

### Task 5: README + roadmap —

## Completion Criteria

- [x] C2/C3/C4 依 design
- [x] `pnpm test` + `pnpm typecheck`
- [x] 不 bump version
