# v0.15 感知包 Implementation Plan

**Status:** ✅ 完成（2026-09-16）· branch `cursor/v0.15-presence` · commit `0f3da0a`

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `watch` 加上等待高亮（P1）、context 血條（P2）、picker 忙／等／閒前綴（P3）。

**Architecture:** 純函式集中在 `session-presence.ts` 與 `context-snapshot.ts`；`App.tsx` 只負責邊沿響鈴與 banner 優先顯示；`session-preference` 把 presence 前綴編進 label。

**Tech Stack:** TypeScript、Node test runner、Ink／React（既有）

**Spec:** `docs/superpowers/specs/2026-09-16-v0.15-presence-design.md`

## Global Constraints

- 不改 hook／schema 持久化
- 閒置閾值固定 `60_000` ms
- Context 分母固定 `CONTEXT_WINDOW_TOKENS = 1_000_000`
- 無新快捷鍵；不 bump version（非 release）
- 提交：僅在使用者要求時 commit

---

### Task 1: `session-presence` 純函式

**Files:**
- Create: `src/session-presence.ts`
- Create: `src/session-presence.test.ts`

**Interfaces:**
- Produces:
  - `export type SessionPresence = "waiting" | "busy" | "idle"`
  - `export const IDLE_MS = 60_000`
  - `isWaitingForUser(activity?: { toolName: string; phase: string } | null): boolean`
  - `classifyPresence(input: { activity?: { toolName: string; phase: string } | null; updatedAt: string; now?: number }): SessionPresence`
  - `aggregatePresence(items: SessionPresence[]): SessionPresence`
  - `presenceLabelPrefix(presence: SessionPresence): string` → `"! "` | `"● "` | `"○ "`
  - `waitingBannerMessage(toolName: string): string | undefined`
  - `waitingEdgeKey(sessionId: string, activity: { toolName: string; at: string }): string`
  - `shouldRingWaitingBell(prevKey: string | undefined, nextKey: string | undefined): boolean`

- [x] **Step 1: Write failing tests** in `src/session-presence.test.ts` covering design checklist (waiting tools, classify 三態與 60s 邊界、aggregate、prefix、banner、edge bell).

- [x] **Step 2: Run** `node --import tsx --test src/session-presence.test.ts` — expect FAIL (module missing).

- [x] **Step 3: Implement** `src/session-presence.ts` to match design.

- [x] **Step 4: Re-run tests** — expect PASS.

---

### Task 2: Context gauge 字串

**Files:**
- Modify: `src/context-snapshot.ts`
- Modify: `src/context-snapshot.test.ts`

**Interfaces:**
- Consumes: `CONTEXT_WINDOW_TOKENS`
- Produces:
  - `contextOccupancyPct(lastOccupiedTokens: number): number`
  - `formatContextGaugeBar(lastOccupiedTokens: number | undefined): { bar: string; color: "green" | "yellow" | "red" } | undefined`
  - Bar width 24, `█`／`░`; color: pct≥95 red, ≥80 yellow, else green

- [x] **Step 1: Failing tests** for undefined → undefined；500_000 → ~50% green；800_000 yellow；950_000 red.

- [x] **Step 2: Run tests** — FAIL.

- [x] **Step 3: Implement** helpers in `context-snapshot.ts`.

- [x] **Step 4: Run tests** — PASS.

---

### Task 3: Session／專案 label 前綴

**Files:**
- Modify: `src/session-preference.ts`
- Modify: `src/session-preference.test.ts`

**Interfaces:**
- Consumes: `classifyPresence`, `aggregatePresence`, `presenceLabelPrefix`
- Extends `SessionHint` with optional `activityToolName?`, `activityPhase?`
- `formatSessionLabel`／`sessionChoices*`／`projectChoices` 在 label 最前加 prefix

- [x] **Step 1: Failing tests** — waiting session label starts with `! `；busy `● `；idle `○ `；project with one waiting → `! `.

- [x] **Step 2–4:** implement + pass.

---

### Task 4: App 接 P1 + hintsFor

**Files:**
- Modify: `src/ui/App.tsx`
- Optional extract: pure helpers already in session-presence（bell edge）

**Behavior:**
- `hintsFor` 填 `activityToolName`／`activityPhase`
- 選中 session 且 `isWaitingForUser`：顯示 waiting banner（蓋過 notice 文字）、`shouldRingWaitingBell` 為 true 時 `\x07`
- `withNotice(waitingMessage ?? notice, …)`

- [x] **Step 1:** 若有可測純函式已在 Task 1；App 以手動／既有模式接線。
- [x] **Step 2:** 實作 App 接線。
- [x] **Step 3:** `pnpm test` 全綠；`pnpm typecheck` 通過。

---

### Task 5: TaskList 血條 + README

**Files:**
- Modify: `src/ui/TaskList.tsx`
- Modify: `src/ui/App.tsx`（傳 `lastOccupiedTokens` 或 preformatted gauge）
- Modify: `README.md`（等待 banner、血條、`!`／`●`／`○`）

- [x] **Step 1:** `contextSnapshot` 擴充可選 `gauge?: { bar: string; color: "green" | "yellow" | "red" }`
- [x] **Step 2:** TaskList 在 occupiedLine 上方畫彩色 bar
- [x] **Step 3:** README 補使用說明 2–4 句
- [x] **Step 4:** `pnpm test` + `pnpm typecheck`

---

## Spec coverage

| Spec | Task |
|------|------|
| P1 waiting + bell edge | 1, 4 |
| P2 gauge | 2, 5 |
| P3 prefixes + project aggregate | 1, 3, 4 |
| README | 5 |
| 不改 hook | — |

## Completion Criteria

- [x] P1／P2／P3 依 design 實作
- [x] `pnpm test` 194 pass
- [x] `pnpm typecheck` pass
- [x] README 已補血條／等待／列表前綴說明
- [x] 未 bump version（非 release）
- [x] 已 commit／push：`cursor/v0.15-presence` @ `0f3da0a`

### 變更檔案

- `src/session-presence.ts` + `.test.ts`（新）
- `src/context-snapshot.ts` + `.test.ts`
- `src/session-preference.ts` + `.test.ts`
- `src/ui/App.tsx`、`src/ui/TaskList.tsx`
- `README.md`
- specs／plans：`2026-09-16-v0.15-presence*`
