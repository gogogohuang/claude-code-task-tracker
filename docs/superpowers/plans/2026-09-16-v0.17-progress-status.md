# v0.17 Progress + Status Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 E1 `status` CLI、T1 phase 進度條、T2 session 結束提示、T4「下一個該做」。

**Architecture:** 純函式（phase / ended / next）+ 薄 CLI command；TUI 在 TaskList／App 接線。presence 重用 `classifyPresence`。

**Tech Stack:** TypeScript、node:test、Commander、Ink（僅 watch）

**Spec:** `docs/superpowers/specs/2026-09-16-v0.17-progress-status-design.md`

## Global Constraints

- ENDED_MS = 300_000；T2 不落盤；不 bump version；不改 hook
- status 不啟動 Ink；無 session 印 `none`、exit 0
- 測試檔須被 `package.json` `test` script 涵蓋（`src/commands/*.test.ts` 若新增則加入 glob）
- commit 僅在使用者要求時

---

### Task 1: phaseProgress

**Files:**
- Create: `src/workflow/phase-progress.ts`
- Test: `src/workflow/phase-progress.test.ts`

**Produces:** `phaseProgress(run: WorkflowRun | undefined): { done: number; total: number } | undefined`

- [ ] **Step 1: Write failing tests**

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { phaseProgress } from "./phase-progress.js";

test("無 workflow → undefined", () => {
  assert.equal(phaseProgress(undefined), undefined);
});

test("空 phases → undefined", () => {
  assert.equal(
    phaseProgress({ runId: "r", journalPath: "j", phases: [] }),
    undefined,
  );
});

test("3 phase 2 completed → 2/3", () => {
  assert.deepEqual(
    phaseProgress({
      runId: "r",
      journalPath: "j",
      phases: [
        { title: "a", status: "completed" },
        { title: "b", status: "completed" },
        { title: "c", status: "in_progress" },
      ],
    }),
    { done: 2, total: 3 },
  );
});
```

- [ ] **Step 2: Run — expect FAIL (module missing)**
- [ ] **Step 3: Implement**

```ts
import type { WorkflowRun } from "../schema.js";

export function phaseProgress(
  run: WorkflowRun | undefined,
): { done: number; total: number } | undefined {
  if (!run || run.phases.length === 0) return undefined;
  const done = run.phases.filter((p) => p.status === "completed").length;
  return { done, total: run.phases.length };
}
```

- [ ] **Step 4: Run — expect PASS**

---

### Task 2: session-ended

**Files:**
- Create: `src/session-ended.ts`
- Test: `src/session-ended.test.ts`

**Produces:** `ENDED_MS`, `isSessionEnded(state, now?)`, `formatEndedSummary(state)`

- [ ] **Step 1: Failing tests** — 邊界 5 分鐘；running activity → false；in_progress task/todo/phase → false；文案含「似乎已結束」
- [ ] **Step 2–4:** 實作依 design 三條件；`formatEndedSummary` 用 `activityLineLabel` 或「無活動」；無 task/todo 時 `任務 —`

---

### Task 3: pickNextTask

**Files:**
- Create: `src/next-task.ts`
- Test: `src/next-task.test.ts`

**Produces:** `pickNextTask(state): { label: string } | undefined`

- [ ] **Step 1: Failing tests** — 跳過 blockedBy；tasks（id 排序）優先於 todos（陣列序）；無 pending → undefined；label 同 task-rows（subject／content，in_progress 不用 activeForm，因只挑 pending）
- [ ] **Step 2–4:** 實作

---

### Task 4: status CLI

**Files:**
- Create: `src/commands/status.ts`, `src/commands/status.test.ts`
- Modify: `src/cli.tsx`（註冊 command）
- Modify: `package.json` `scripts.test` 加 `src/commands/*.test.ts`

**Produces:** `runStatus(opts): number`；`formatStatusLine`／`statusPayload`

行為：
- 無 `--session`：`loadSessionHints` + `pickPreferredSession(hints, cwd)`；無則印 `none` / `{ sessionId: null }`
- 有 `--session`：`resolveSessionId`；找不到 stderr + exit 1
- 一行：`{presence} · {shortId} · ○ {done}/{total} · {activity}`（任務數／活動可省略）
- `--json`：`{ sessionId, presence, done, total, activitySummary, cwd }`

- [ ] **Step 1–4:** TDD 後接 `cli.tsx`

---

### Task 5: TaskList / App / README

**Files:**
- Modify: `src/ui/TaskList.tsx` — phase bar（tasks bar 上方）、ended banner、下一個一行
- Modify: `src/ui/App.tsx` — 傳 `endedSummary`（用 `clockRevision`／`Date.now()`）
- Modify: `README.md` — `status` 用法

- [ ] Wire props；無 Ink 測試則靠 typecheck
- [ ] `pnpm test` + `pnpm typecheck`

## Completion Criteria

- [x] E1/T1/T2/T4 依 design
- [x] tests + typecheck pass（229 tests）
- [x] README 含 status
- [x] 不 bump version
