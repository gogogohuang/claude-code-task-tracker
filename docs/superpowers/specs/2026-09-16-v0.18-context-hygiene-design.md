# v0.18 Design — Context Hygiene（C2 + C3 + C4）

日期：2026-09-16  
狀態：已核准（plan 核准後實作）  
對應 roadmap：`docs/superpowers/specs/2026-09-16-tui-feature-roadmap.md` v0.18  

## 目標

1. **C2** `inspect` 清單依檔案 byte 在同 section 內降序，列上顯示體積  
2. **C3** Advice 面板可捲動減肥項目：過肥 tool_result（可帶 path）+ 同 path 反覆 Read（≥3）  
3. **C4** `heavy-baseline` 建議下嵌 launch Top-5 熱力摘要  

## 非目標

- C1 複製、新 WatchView、行數熱力、bump version、改 hook／CLAUDE.md  

## 決策（寫死）

| 項 | 值 |
|----|-----|
| UI | C3／C4 進既有 `a` AdvicePanel |
| 熱力 | `statSync` byte；不讀全文 |
| Top-N | 5（launch、有 byteSize） |
| 反覆 Read | 同 path ≥ 3 次觸發一次 |
| 過肥門檻 | 維持既有 30000 字元 |

## 模組

- `src/inspect/heat.ts`：`formatByteSize`、`sortEntriesByHeat`、`heatSummaryLines`  
- `InspectEntry.byteSize?`；discover 後 enrich + sort  
- `ToolResultChars.path?`；toolUse map 存 `{ name, path? }`  
- `SessionUsageStats.readPathCounts`；`AdviceKind` 加 `repeated-read`；`Advice.detailLines?`  

## 測試

- heat 排序／格式／Top-5  
- path 解析 + fat 文案帶 path  
- repeated-read 邊界 3  
- heavy-baseline detail 降級  

## 核准

已鎖定：Advice 面板、byte 熱力、過肥 + 反覆 Read。
