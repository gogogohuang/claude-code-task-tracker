@AGENTS.md

## Claude Code notes

- Stage handoff: after writing the progress file, `/clear` (or start a new session), then `Read` that file.
- Subagents: use the `Agent` tool and follow the return guidance in "Keep the Main Thread Light".
